package Bioskop.repositories;

import Bioskop.models.PaketiHrane;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PaketiHraneRepository extends JpaRepository<PaketiHrane, Long> {
}
